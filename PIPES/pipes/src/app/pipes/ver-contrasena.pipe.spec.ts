import { VerContrasenaPipe } from './ver-contrasena.pipe';

describe('VerContrasenaPipe', () => {
  it('create an instance', () => {
    const pipe = new VerContrasenaPipe();
    expect(pipe).toBeTruthy();
  });
});
